import styled from "@emotion/styled";


const UserMenuContainer = styled.div`
  display: flex;
  align-Items: center;
`;

const UserName = styled.span`
  font-weight: 700;
  margin-right: 12px;
`;

export {UserMenuContainer, UserName };
